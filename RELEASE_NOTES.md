# 多平台采集工作台 v2.1.2

## 采集误停修复

- 修复账号页面的内嵌 iframe 重定向被误判为主页面离开平台，导致采集立即停止的问题；小红书和抖音均适用。
- 正常内嵌页面跳转不再中断采集或清空待处理响应；主页面真正跳到平台外仍会被阻止并停止任务。
- 兼容 Electron 新版导航事件字段；真正跨平台的停止提示仅显示目标域名，不暴露地址中的查询令牌。
- 增加 Windows/macOS 的真实 Electron 隔离回归：平台内主页面重定向、内嵌跨域重定向后继续采集、主页面跨平台跳转保护。
- 未访问用户账号或本地会话数据；目标电脑的真实账号仍需升级后复测。

## 初始化修复

- 修复新建抖音账号时，早到的页面导航或状态事件导致工作区误用小红书默认链接的问题。
- 尚未登记或已移除账号的页面事件不再创建工作区，防止旧事件恢复已删除的数据空间。

## 抖音评论接入

- 添加账号时可选择“抖音”，创建独立登录空间；原有小红书账号、登录会话和采集数据保持不变。
- 抖音账号显示“视频评论”，输入完整公开作品链接即可启动，不要求作品归当前账号所有。
- 只监听目标视频在正常网页浏览中加载的评论及回复，支持评论点赞数、回复数、地区和时间；未提供的指标不填造。
- 评论按视频归入卡片，沿用导出、情绪分类、高互动举例及 AI 舆情总结。
- 遇到登录提示、验证码、访问限制或离开目标视频时停止采集，并保留已有数据。
- 首版需在左侧打开评论区；能够识别评论容器时低频滚动，否则可手动滚动加载。仅采集当前账号正常可见的数据，不保证所有公开作品或全部评论都可访问。
- 支持 `https://www.douyin.com/video/视频ID` 完整链接及主页/用户页的 `modal_id` 链接；分享短链接需先打开后复制完整地址。
- 自动化、关键词搜索和作者批量作品采集暂未接入抖音，不展示对应入口。
- 本版通过采集解析、任务隔离、真实 Electron 模拟响应及界面回归；已验证未登录网页识别，登录后真实抖音评论仍需实机验收，平台网页变化可能影响兼容性。

## 延续的 AI 更新

- 评论分析新增“舆情总结”：整体判断、认可与共鸣、争议与顾虑、用户诉求及回应建议；无依据的维度不强行生成。
- 总结可展开查看引用评论，限定当前采集样本，不把评论观点当作事实或推断平台整体舆情。
- 放大 AI 分析入口，滚动时保留操作栏；分析中显示旋转图标、流动状态条、真实处理阶段、批次和耗时，支持减少动态效果。
- 舆情总结失败时保留已完成的情绪分析；取消和重新分析失败不覆盖上次完整报告。
- 新总结只在用户确认后生成，使用匿名关键文本及有界摘录，可能增加一次模型调用。

## 延续功能

- 新增“设置 → 通用 → 软件更新”：启动后自动检查新版，支持手动检查、下载进度及确认后安装重启。
- Windows 使用 GitHub 官方发布源和安装包 SHA-512 校验；下载后不会自动重启，运行任务期间禁止安装更新。
- 主分支版本号递增后，GitHub 自动测试、构建 Windows/macOS 安装包并发布新版本及在线更新清单。
- 修复 AI 连接测试将中转站流式响应误判为 JSON 的问题，兼容 Responses / Chat Completions 和不准确的响应头。
- AI 设置支持“保存并测试”，模型列表不可用不阻止手动配置，按钮和结果固定在弹窗底部。

- 软件名称右侧显示版本号，与安装包共用项目版本配置。
- 精简工作台布局，数据看板支持拖动调整宽度和内容缩放。
- 搜索及作者笔记列表支持逐条采集评论；已有评论显示“已采集”，点击直接查看对应笔记评论。
- 评论按笔记归入卡片，支持查看原文、统计分析、重新采集及删除本地评论。
- 增加正向、负向、中性情绪分析，以及高讨论、高点赞评论举例；旧评论可重新采集补齐互动指标。
- 将 AI 服务配置和通用设置集中到右上角“…”中的“设置”，隐藏 Windows 顶部英文菜单。
- 统一“自动化”“笔记评论”的菜单名称、任务标题和添加提示。
- 修复评论采集状态、点赞/回复数更新及停止任务后的状态同步。
- Windows 安装向导支持安装目录和桌面快捷方式选择，升级默认保留账号及本地数据。

## 安装包

- Windows x64：`xiaohongshu-multi-account-2.1.2-x64.exe`。
- macOS Apple Silicon：`xiaohongshu-multi-account-2.1.2-arm64.dmg`；当前通过发布页下载安装。
- 安装包不包含用户的账号会话、Cookie、采集结果或 AI 密钥。
- Windows 安装包尚未配置代码签名，可能显示“未知发布者”。

## 注意事项

- v2.0.5 及更早版本需手动安装本版一次，此后 Windows 版可在软件内升级。

- 同一账号一次只能运行一篇笔记的评论采集；批量加入卡片不会自动逐篇执行。
- AI 分析需配置服务并由用户主动发起，可能产生服务商调用费用；结果仅供参考。
- 请仅采集你有权访问和使用的公开数据，并遵守平台规则。
